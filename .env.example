# Text LLM (required)
LLM_API_KEY=your_text_llm_api_key
LLM_MODEL=your_text_model
LLM_API_BASE=https://your-text-provider.example/v1

# Image generation for the daily cover (optional)
# Leave IMAGE_API_KEY empty only when you want the local cover fallback.
IMAGE_API_KEY=your_image_api_key
IMAGE_MODEL=your_image_model
IMAGE_API_BASE=https://your-image-provider.example/v1

# WeChat Official Account (required to create drafts)
WECHAT_APP_ID=wx_your_app_id
WECHAT_APP_SECRET=your_app_secret
WECHAT_TOKEN=your_custom_token_here

# Public daily page
DOMAIN=example.com
PAGES_URL=https://example.com

# Optional tuning lives in .env.advanced.example.
# Copy only the settings you need into this .env file.
